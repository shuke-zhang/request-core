1. 二次封装的请求方式，0.0.1版本仅仅支持web端
2. `pnpm add axios`
3. `pnpm add qs`

> 发布流程
>
> 1. `pnpm -F "@shuke~/request" bumpp patch` 更新当前项目包版本号
> 2. `pnpm publish` 先提交代码再运行命令。将会自动使用脚本打包并发布

4. 0.0.1 版本

   > 添加 shared.d.ts

5. 0.0.4 版本 构建依赖
